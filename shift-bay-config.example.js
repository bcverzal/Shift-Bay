window.SHIFT_BAY_CONFIG = {
  // Local server mode: leave apiBase blank so calls use /api/... on localhost.
  // Hosted mode: set this to the Supabase Edge Function URL, for example:
  // https://YOUR_PROJECT_REF.supabase.co/functions/v1/shift-bay-api
  apiBase: "",

  // Optional display/debug fields. These are public values only.
  supabaseUrl: "",
  locationId: ""
};
