# Legacy name retained for existing shortcuts. All local launches use the
# verified launcher so an already-running server can never be mistaken for it.
& (Join-Path $PSScriptRoot "Launch Shift Bay Local.ps1")
